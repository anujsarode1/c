#include <stdio.h>

int compare() {
    int compare; 
    scanf("%d", &compare); 
    scanf("%d", &compare); 


    if (compare == 1) {  
        printf("student one\n");
    } else { 
        printf("student two\n");
    }

    return 0;
}

int main() {
    compare(); 
    return 0;
}
// not running